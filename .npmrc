# Ensure consistent installs without package-lock.json
package-lock=false
save-exact=true
fund=false
audit=false

# Use npm cache for better performance
cache=/tmp/npm-cache

# Reduce noise in CI logs
loglevel=warn
